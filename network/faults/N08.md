# N08 默认路由丢失

状态：未执行；以下为演练方案，非故障处理实绩。

前置：另存拓扑快照，导出配置；基线测试通过；只在隔离实验环境操作。

1. 注入：删除HQ-AR1到198.51.100.2的默认路由。
2. 预期现象：OSPF优先默认路由撤销；新流量走AR2。
3. 取证：display ip routing-table；tracert；NAT表。先留失败证据，再改配置。
4. 回退：恢复AR1默认路由；观察原NAT会话中断。
5. 验收：重复原业务测试与隔离反向测试，保存恢复后的状态。

| 记录项 | 实测填写 |
|---|---|
| 操作者、VRP版本、拓扑版本 | 待填 |
| 开始时间、时区、故障注入时间 | 待填 |
| 现象与逐层排查依据 | 待填 |
| 原因；排除过的其他原因 | 待填 |
| 最后成功、首个失败、连续3次恢复成功时间 | 待填 |
| 探测间隔、总包数、丢包数、失败区间 | 待填 |
| 回退命令、恢复后业务与隔离验证 | 待填 |
| 原始日志/抓包/截图相对路径 | 待填 |

不要把“丢包数 × 间隔”当作精确协议收敛时间。分别记录观测到的业务中断区间与控制面事件；每个案例至少重复3次并报告范围。

